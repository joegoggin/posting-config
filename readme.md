# Posting Config

My config for API client [posting](https://posting.sh).

## Theme

The theme file `matrix.toml` won't work if it is just in the config directory.
To find out where this file should be placed run the following command:

```
posting locate themes
```
